meta {
  name: echo-req copy
  type: http
  seq: 2
}

post {
  url: http://localhost:3000/users-api
  body: json
  auth: bearer
}

auth:bearer {
  token: 123habmnbds
}

body:json {
  {
    "name":"Bruno"
  }
}

assert {
  res.status: eq 200
  res.body: isJson
}

tests {
  test("return JWT token", function () {
    expect(res.getStatus()).to.equal(200);
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
