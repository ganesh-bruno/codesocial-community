info:
  name: echo-req-2
  type: http
  seq: 3

http:
  method: POST
  url: https://echo.usebruno.com
  body:
    type: json
    data: |-
      {
        "name":"usebruno",
        "description":"The most loving API client built for developers"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
